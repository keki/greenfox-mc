/* global describe, it, expect */

import di from '../container';

// import Memory from './implemenetations/memory'

describe('Cache', () => {
    it('should be a factory', () => {
        console.log(di.container)
    })

    // describe('In memory implementation', () => {
    //     it('should work', () => {
    //         console.log(di.get);
    //         const cache = new Cache(di.get(''))
    //     })
    // })
})